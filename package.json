{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tinacms dev -c 'jekyll serve --watch'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tinacms build"
  },
  "dependencies": {
    "sass": "^1.78.0",
    "tinacms": "^2.2.3",
    "@tinacms/cli": "^1.6.3"
  }
}
